<input type="{{ $props['type'] ?? 'text' }}" id="{{ $props['id'] }}" name="{{ $props['name'] }}"
    placeholder="{{ $props['placeholder'] ?? '' }}" value="{{ $props['value'] }}"
    class="{{ $props['class'] ?? '' }} rounded-md text-sm text-gray-800 tracking-wide border border-cultured placeholder:text-gray-400 px-4 py-2 focus:border-emerald-500 outline-none w-full">
